services:

  web: 
    image: node:20-alpine
    working_dir: /app
    ports: 
      - 3000:3000
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - WATCHPACK_POLLING=true
      - CHOKIDAR_USEPOLLING=true
      - NODE_ENV=development
    command: sh -c "npm install && npm run dev"

  database:
    image: postgres:16
    
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=mypassword
      - POSTGRES_DB=kepler-space-lab
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    healthcheck:
      test: pg_isready -U user -d kepler-space-lab
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s

  adminer: 
    image: adminer
    ports:
      - 8080:8080
    depends_on:
      - database